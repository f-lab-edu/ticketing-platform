server:
  tomcat:
    accept-count: 100
    threads:
      max: 200
      min-spare: 50

spring:
  application:
    name: ticket-service
  jpa:
    hibernate:
      ddl-auto: create-drop

redis:
  timeout: 3s
  lock:
    wait-time: 5s
    lease-time: 3s

queue:
  max-waiting-count: 20000
  entry-timeout: 5m
  waiting-timeout: 30m
  sse-timeout: 10m
  position-broadcast-interval: 5s

processing:
  max-count: 200

sse:
  thread-pool:
    core-size: 10
    max-size: 50
    queue-capacity: 100
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus

ticket:
  lock:
    strategy: pessimistic

seat:
  hold-timeout: 5m
  hold-check-interval: 30s

jwt:
  secret: your-256-bit-secret-key-for-queue-token-generation-must-be-long-enough
  expiration: 10m
