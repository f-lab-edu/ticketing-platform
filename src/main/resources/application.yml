spring:
  application:
    name: ticket-service
  jpa:
    hibernate:
      ddl-auto: create-drop

redis:
  timeout: 3000
  lock:
    wait-time: 5
    lease-time: 3

queue:
  max-processing-count: 100
  entry-timeout: 5m
  waiting-timeout: 30m
  sse-timeout: 10m
  position-broadcast-interval-ms: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus

ticket:
  lock:
    strategy: pessimistic
