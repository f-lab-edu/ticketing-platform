spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ticket_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
#        format_sql: true

redis:
  host: localhost
  port: 6379
  timeout: 3000
  lock:
    wait-time: 5
    lease-time: 3

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
